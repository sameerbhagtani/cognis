meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/health
  body: none
  auth: none
}

docs {
  Public. Registered before the auth middleware, so it answers without a session.
}
