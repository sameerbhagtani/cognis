import { Text, View } from "react-native";
import { dark } from "@cognis/constants";

console.log(dark);

export default function Index() {
    return (
        <View>
            <Text>Cognis: Mobile App</Text>
        </View>
    );
}
